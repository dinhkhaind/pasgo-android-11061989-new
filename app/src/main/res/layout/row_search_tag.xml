<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="@dimen/tag_lv_margin"
    android:paddingTop="@dimen/tag_lv_margin"
    android:gravity="center"
    android:id="@+id/lnNhomKhuyenMai"
    android:background="@color/bg_layout">

    <TextView
        android:id="@+id/tvName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:minLines="2"
        android:maxLines="2"
        android:padding="@dimen/search_tag_item_pading"
        android:text="Nhà hàng \nsang trọng"
        android:textColor="@color/link_text"
        android:textSize="@dimen/tag_item_tv_size"
        android:background="@drawable/bg_vien_search_tag"/>
</LinearLayout>